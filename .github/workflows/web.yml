name: Web App CI

on:
  push:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Check web files
        run: |
          test -f index.html
          test -f styles.css
          test -f script.js
          echo "All web files are present."
